/**
 * Left-hand navigation bar (outside main container).
 */
export default function SideNavigationPane() {
  return (
    <nav className="flex h-full w-16 flex-col items-center justify-start rounded-lg border border-neutral-700 bg-neutral-800/60 p-2 backdrop-blur">
      {/* TODO: add nav icons/buttons */}
    </nav>
  );
}
